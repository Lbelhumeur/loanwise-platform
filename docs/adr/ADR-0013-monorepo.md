# ADR-0013: Monorepo

**Status:** Accepted

LoanWise uses an npm-workspaces monorepo so frontend, backend, domain models, UI primitives, and configuration remain versioned together while deployable applications build independently.
