# LW-0002 — LoanWise Monorepo & Workspace Foundation

Merge this package into the existing `loanwise-platform` repository root. Do not create an LW-0002 directory inside the repository.

Goals: npm workspaces, reusable packages, tenant-aware domain types, TypeScript conventions, and independent CI/CD build targets.

Commands: `npm install`, `npm run typecheck`, `npm run test`, `npm run validate`.

Keep the original LoanWise recovery archive outside Git until its contents and provenance are reviewed.
