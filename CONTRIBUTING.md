# Contributing

Use feature branches named for work items, e.g. `feature/LW-0001-bootstrap`.

Before a pull request:
- type check
- test
- build
- update documentation when behavior or architecture changes
